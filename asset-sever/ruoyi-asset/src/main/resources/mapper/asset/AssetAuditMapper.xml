<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.asset.mapper.AssetAuditMapper">

    <insert id="insertAuditFromPurchase">
        INSERT INTO asset_audit (
            purchase_id,
            asset_name,
            purchase_price,
            purchase_num,
            model,
            spec,
            submit_status,
            audit_status,
            in_status,
            remark,
            del_flag,
            create_time
        )
        SELECT
            purchase_id,
            asset_name,
            purchase_price,
            purchase_num,
            model,
            spec,
            '1',
            '0',
            in_status,
            remark,
            '0',
            NOW()
        FROM asset_purchase
        WHERE purchase_id IN
        <foreach collection="purchaseIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND del_flag = '0'
    </insert>

    <select id="queryPurchaseIdsByAuditIds" resultType="java.lang.Long">
        SELECT
            purchase_id
        FROM asset_audit
        WHERE audit_id IN
        <foreach collection="auditIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
         AND del_flag = '0'
    </select>

</mapper>